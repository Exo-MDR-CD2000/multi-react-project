// create helper function that returns the current year for the footer.

/**
 * Helper function that returns the current year for the footer.
 * @returns {number} The current year.
 */
const FooterCurrentYear = () => {
    return new Date().getFullYear();
};

export default FooterCurrentYear;